## Packages
react-icons | Brand icons for the tech stack section (already in package.json but ensuring it's available)
embla-carousel-react | For the project gallery slider (already in package.json)
framer-motion | For beautiful scroll animations (already in package.json)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}
Added smooth scroll behavior for anchor links.
Stock images from Unsplash are used with descriptive HTML comments.
